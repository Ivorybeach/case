ssm做的知通网站
值得一提的是，这个项目有个猫脸识别登录功能，可以调用电脑摄像头，如果识别到是猫脸，就可以成功登录
猫脸识别是一个python脚本，用python的openCV模块实现的，用的是openCV自带的特征识别器，准确率堪忧

java调用python脚本：

	Runtime:
		代表java application正在运行时的类，可以通过获取此类实例，即可在java application运行时执行其他外部程序
	
	Processor:
		执行其他程序，并且能获取其他程序的状态，是否执行完毕，通过输入流、输出流来获取程序运行的结果
		
