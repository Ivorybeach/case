ssm搭的知通网站
值得提一嘴的是，java调用了一个猫脸识别的python脚本。在Mr.Cat的登录的时候，会调用摄像头，如果是猫脸，就可以认证成功并登录。
猫脸识别使用python的opencv模块写的，使用的特征识别器为opencv自带，准确率堪忧。

java调用python脚本：

	Runtime:
		代表java application正在运行时的类，可以通过获取此类实例，即可在java application运行时执行其他外部程序
	
	Processor:
		执行其他程序，并且能获取其他程序的状态，是否执行完毕，通过输入流、输出流来获取程序运行的结果
		
